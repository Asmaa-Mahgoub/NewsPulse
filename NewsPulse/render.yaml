services:
  - type: web
    name: newspulse
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
    startCommand: gunicorn NewsPulse.wsgi:application
    postDeployCommand: |
      python manage.py migrate
      python manage.py createsu
